services:
  streamlit-app:
    build:
      context: ./frontend  
    container_name: streamlit-app
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    networks:
      - app-network-st


networks:
  app-network-st:
    external: true